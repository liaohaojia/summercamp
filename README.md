# 26暑假衔接学习计划

这是一个可直接部署到 GitHub Pages 的静态网页项目，内容来自：

- `D:\Users\lenovo\Desktop\c++.md`
- `D:\Users\lenovo\Desktop\高等数学上册.md`

## 本地文件

- `index.html`：页面结构
- `styles.css`：页面样式
- `script.js`：课程数据与交互

## 部署到 GitHub Pages

1. 新建一个 GitHub 仓库。
2. 把当前目录文件上传到仓库根目录。
3. 在 GitHub 仓库中打开 `Settings -> Pages`。
4. `Build and deployment` 选择 `Deploy from a branch`。
5. 分支选择 `main`，目录选择 `/ (root)`。
6. 保存后等待 GitHub Pages 发布完成。

如果仓库名不是 `<username>.github.io`，最终地址通常是：

`https://<username>.github.io/<repository-name>/`
